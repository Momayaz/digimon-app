<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Digimon</title>
    <link rel="stylesheet" href="../style.css">
</head>
<body id="homeBody">
    <header>
        <h1></h1>
    </header>
    <main>
        <%result.forEach(element=>{%>
            <section>
                <h2 id="homeName"><%=element.name%></h2>
                <img src="<%=element.img%>" alt="" id="homeImage">
                <P id="homeP"><%=element.level%></P>
                <form action="/adding" method="POST">
                    <input type="hidden" name="name" value="<%=element.name%>">
                    <input type="hidden" name="img" value="<%=element.img%>">
                    <input type="hidden" name="level" value="<%=element.level%>">
                    <button id="homeButton">ADD TO FAVORITE</button>
                </form>
                <p>==============================<>========================</p>
            </section>
        <%})%>
        
    </main>
</body>
</html>